Patch and maintenance scripts for the live EPRA wrapper.

Rules:
- Put one-purpose patch scripts here.
- Run them from the EPRA_API_Wrapper_v2 root.
- Do not store backups here; backups go to Checkpoints/.
- Prefer named scripts here over long inline heredocs in shell.
